package be.cytomine.appstore.models.task;

public enum ValueType {
    BOOLEAN,
    INTEGER,
    NUMBER,
    STRING,
    ENUMERATION,
    DATETIME,
    GEOMETRY,
    IMAGE,
    WSI,
    FILE,
    ARRAY
}
