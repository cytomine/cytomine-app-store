apiVersion: apps/v1
kind: Deployment
metadata:
  name: appstore-backend
  labels:
    app: appstore-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appstore-backend
  template:
    metadata:
      labels:
        app: appstore-backend
      annotations:
        checksum/config: "{{ include (print $.Template.BasePath "/backend/appstore-backend-config.yaml") . | sha256sum }}"
    spec:
      containers:
      - name: appstore-backend
        image: "{{ .Values.appstore_backend.image }}"
        ports:
        - containerPort: {{ .Values.appstore_backend.port }}
        envFrom:
        - configMapRef:
            name: appstore-backend
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-appstore-backend-secret
              key: password
