apiVersion: v1
kind: Service
metadata:
  name: postgres-appstore-backend
spec:
  clusterIP: None
  selector:
    app: postgres-appstore-backend
  ports:
    - name: postgresql
      port: {{ .Values.postgresql.port }}
      protocol: TCP
